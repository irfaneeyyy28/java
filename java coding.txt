public class LoopsAndConditions {
    public static void main(String[] args) {
        // Variable declarations
        int maxCount = 5;
        String message = "Current count: ";

                       System.out.println(message + i + " (Even)");
            } else {
                System.out.println(message + i + " (Odd)");
            }
        }
    }
}
